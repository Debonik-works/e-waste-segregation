# Example environment for local backend runs (copy to .env).
HOST=0.0.0.0
PORT=8080
CORS_ORIGINS=*
MODEL_PATH=model/best.pt
CONFIDENCE_THRESHOLD=0.50
INFERENCE_IMGSZ=640
INFERENCE_DEVICE=auto
SERIAL_ENABLED=false
SERIAL_PORT=
SERIAL_BAUD=9600
MOTOR_DURATION_MS=1500
MOTOR_SPEED=200
